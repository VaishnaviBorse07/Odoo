# Python 3.14: use current releases with cp314 wheels (avoid pydantic-core from-source builds)
fastapi>=0.115.6
uvicorn[standard]>=0.32.0
sqlalchemy>=2.0.36
asyncpg>=0.30.0
alembic>=1.14.0

# Auth (passwords: plain-text compare in dev — see app.core.security)
python-jose[cryptography]>=3.3.0
python-multipart>=0.0.9

# Validation / settings
pydantic>=2.10.0
pydantic-settings>=2.7.0
email-validator>=2.2.0

# HTTP client (currency + countries API)
httpx>=0.27.0

# OCR (Pillow 10.x fails to build on 3.14)
pytesseract>=0.3.10
Pillow>=11.2.0
pdf2image>=1.17.0

# CORS / utilities
python-dotenv>=1.0.1
